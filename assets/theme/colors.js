const colors = {
  primary: (opacity = 1) => `rgba(80, 120, 255, ${opacity})`,
  grey: (opacity = 1) => `rgba(120,120,120, ${opacity})`,
  white: () => "#FFFFFF",
  black: () => "#000000",
};

export default colors;